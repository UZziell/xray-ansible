UVICORN_UDS = "/run/marzban.socket"
# UVICORN_PORT = {{ uvicorn_port }}
# UVICORN_SSL_CERTFILE = "{{ uvicorn_ssl_certfile }}"
# UVICORN_SSL_KEYFILE = "{{ uvicorn_ssl_keyfile }}"
SQLALCHEMY_DATABASE_URL = "{{ sqlalchemy_database_url }}"
XRAY_JSON = "{{ xray_json }}"
XRAY_FALLBACK_INBOUND_TAG = "{{ xray_fallback_inbound_tag }}"
SUDO_USERNAME = "{{ marzban_sudo_username }}"
SUDO_PASSWORD = "{{ marzban_sudo_password }}"
XRAY_SUBSCRIPTION_URL_PREFIX = "{{ xray_subscription_url_prefix }}"

TELEGRAM_ADMIN_ID = " {{ telegram_admin_id }}"
TELEGRAM_API_TOKEN = "{{ telegram_api_token }}"
